@model IEnumerable<Norget.Models.Usuario>

@{
    ViewData["Title"] = "InfoSp";
}
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/infosp.css" asp-append-version="true" />
    <link rel="shortcut icon" type="imagex/png" href="~/img/logo.ico">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.8.1/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta2/css/all.min.css">
    <title>Informações Pessoais</title>
   
</head>
<body>
    
    <div class="voltar">
     <a asp-controller="Home" asp-action="Index"><i class="fas fa-chevron-left"></i>VOLTAR</a>
    </div>

    <div class="profile-header">
        <div class="profile-pic">
            <i class="fas fa-user"></i>
        </div>
        <br> <br>
        <div class="profile-info">
            @foreach (var usuario in Model)
            {
                <h2>@usuario.NomeCli</h2>
            }
        </div>

        <div class="account-section">
            <hr class="quebra">
            <h2>Minha Conta:</h2>
        </div>

        <div class="cards-section">

            <div class="cards-header">
                <h3>Informações Pessoais:</h3>
            </div>

            <div class="cards-container">

                <div class="card">
                    @foreach (var usuario in Model)
                    {
                        <p><strong>Nome:</strong></p>
                        <p>@usuario.NomeCli</p>
                        <span class="editar">EDITAR</span>
                    }
                </div>

                <div class="card">
                    @foreach (var usuario in Model)
                    {
                        <p><strong>E-Mail:</strong></p>
                        <p>@usuario.EmailCli</p>
                        <span class="editar"> <a asp-controller="Usuario" asp-action="Editar">EDITAR</a></span>
                    }
                </div>

              <!--  <div class="card">                    
                   @foreach (var usuario in Model)
                   {
                       <p><strong>Telefone:</strong></p>
                       <p>@usuario.Tel </p>
                              <span class="editar"> <a asp-controller="Usuario" asp-action="Editar">EDITAR</a></span>
                   }
                </div> -->
                <div class="card">
                 @foreach (var usuario in Model)
                 {
                    <p><strong>Senha:</strong></p>
                    <p>@usuario.SenhaCli</p>
                        <span class="editar"> <a asp-controller="Usuario" asp-action="Editar">EDITAR</a></span>
                 }
               </div>
            </div>
        </div>
    </div>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>